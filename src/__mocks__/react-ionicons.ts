import * as React from "react";
import * as h from "react-hyperscript";

const DefaultComponent: React.StatelessComponent = () => h("div", "Icon");

const Ionicon = DefaultComponent;

export default Ionicon;
